<svg width="200" height="150" viewBox="0 0 200 150" fill="none"
  xmlns="http://www.w3.org/2000/svg">
  <!-- 背景 -->
  <rect width="200" height="150" fill="#FFFFFF"/>

  <!-- 蓝色渐变色块 - 左上 -->
  <defs>
    <radialGradient id="blueGradient" cx="30%" cy="30%" r="60%">
      <stop offset="0%" stop-color="#BFE1FF" stop-opacity="1"/>
      <stop offset="60%" stop-color="#BFE1FF" stop-opacity="0"/>
    </radialGradient>

    <!-- 粉色渐变色块 - 右下 -->
    <radialGradient id="pinkGradient" cx="70%" cy="70%" r="60%">
      <stop offset="0%" stop-color="#FFD7EA" stop-opacity="1"/>
      <stop offset="60%" stop-color="#FFD7EA" stop-opacity="0"/>
    </radialGradient>

    <!-- 模糊滤镜 -->
    <filter id="blur" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="12"/>
    </filter>
  </defs>

  <!-- 蓝色渐变块 -->
  <ellipse cx="20" cy="20" rx="60" ry="60" fill="url(#blueGradient)" filter="url(#blur)"/>

  <!-- 粉色渐变块 -->
  <ellipse cx="180" cy="130" rx="60" ry="60" fill="url(#pinkGradient)" filter="url(#blur)"/>

  <!-- 卡片容器 -->
  <rect x="40" y="25" width="120" height="100" rx="8" fill="rgba(255,255,255,0.98)" stroke="rgba(0,0,0,0.04)" stroke-width="1"/>

  <!-- 卡片阴影 -->
  <rect x="42" y="27" width="120" height="100" rx="8" fill="rgba(21,30,60,0.04)"/>

  <!-- 左侧渐变条 -->
  <defs>
    <linearGradient id="sideGradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="rgba(187,219,255,0.18)"/>
      <stop offset="65%" stop-color="rgba(255,255,255,0)"/>
    </linearGradient>
  </defs>
  <rect x="40" y="25" width="40" height="100" rx="8 0 0 8" fill="url(#sideGradient)"/>

  <!-- 微妙高光条 -->
  <rect x="40" y="37" width="1" height="20" fill="rgba(255,255,255,0.6)"/>

  <!-- 卡片内容 -->
  <!-- 日期 -->
  <text x="50" y="40" font-family="Inter, sans-serif" font-size="6" fill="#64748B" font-weight="400">2025.10.13</text>

  <!-- 标题 -->
  <text x="50" y="52" font-family="Inter, sans-serif" font-size="10" fill="#0B1720" font-weight="600">👋 Hi there</text>

  <!-- 内容行 -->
  <text x="50" y="65" font-family="Inter, sans-serif" font-size="6" fill="#334155">💡 You can try typing text here</text>
  <text x="50" y="73" font-family="Inter, sans-serif" font-size="6" fill="#334155">supporting real-time Markdown</text>

  <!-- 列表项 -->
  <circle cx="52" cy="82" r="1" fill="#60A5FA"/>
  <text x="56" y="84" font-family="Inter, sans-serif" font-size="5" fill="#334155">Copy and paste to insert</text>

  <circle cx="52" cy="89" r="1" fill="#60A5FA"/>
  <text x="56" y="91" font-family="Inter, sans-serif" font-size="5" fill="#334155">Ctrl+I for italic text</text>

  <!-- 作者签名 -->
  <text x="130" y="105" font-family="Inter, sans-serif" font-size="5" fill="#64748B" text-anchor="end">— Ant Card</text>

  <!-- 底部二维码区域 -->
  <rect x="50" y="110" width="100" height="12" rx="4" fill="rgba(250,250,250,0.8)" stroke="rgba(0,0,0,0.06)" stroke-width="0.5"/>

  <!-- 二维码标题 -->
  <text x="53" y="117" font-family="Inter, sans-serif" font-size="5" fill="#0B1720" font-weight="600">Streamer Card</text>
  <text x="53" y="120" font-family="Inter, sans-serif" font-size="4" fill="#94A3B8">Scan QR Code</text>

  <!-- 简化的二维码 -->
  <rect x="135" y="113" width="12" height="6" rx="1" fill="rgba(0,0,0,0.03)"/>
  <rect x="137" y="114" width="8" height="4" fill="#0B1720"/>
  <rect x="138" y="115" width="2" height="2" fill="#FFFFFF"/>
  <rect x="141" y="115" width="2" height="2" fill="#FFFFFF"/>

  <!-- 页脚标签 -->
  <g transform="translate(100, 135)">
    <!-- 菱形图标 -->
    <path d="M0 -3L3 0L0 3L-3 0L0 -3Z" stroke="#9AA3A7" stroke-width="0.5" fill="none"/>
    <text x="6" y="1" font-family="Inter, sans-serif" font-size="5" fill="#9AA3A7" text-anchor="start">Ant Card</text>
  </g>
</svg>